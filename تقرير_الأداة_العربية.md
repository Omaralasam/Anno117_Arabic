# تقرير أداة تعريب Anno 117

## الفكرة

تم تحويل طريقة التعريب الناجحة إلى واجهة واحدة حتى لا يحتاج المستخدم لتشغيل السكربتات يدويًا. الأداة تستخدم نفس المسار الذي ثبت أنه يعمل بعد تحديث اللعبة: أرشيف مدمج، فهرس محدث، checksum صحيح، ونسخة احتياطية قبل التثبيت.

## الخطوات التي تنفذها الأداة

1. بناء أرشيف `file_browse_patterns.rda` مدمج من أرشيف اللعبة الأصلي مع ملفات التعريب.
2. توليد `file.db` و `checksum.db` المؤقتين من الحزمة العربية.
3. تحديث الفهرس الحالي للعبة بحيث يستخدم فتحة `file_browse_patterns.rda` الموجودة بدل إضافة أرشيف جديد.
4. تثبيت الملفات في مجلد `maindata`.
5. إنشاء نسخة احتياطية تلقائية داخل `backups\existing-slot-arabic`.

## الملفات الأساسية

- `rda-work\build_merged_existing_slot_rda.ps1`
- `rda-work\generate-filedb-for-direct-arabic.ps1`
- `rda-work\patch_filedb_existing_slot.py`
- `install_existing_slot_arabic.ps1`
- `restore_existing_slot_arabic.ps1`

## معيار النجاح

- اللعبة تعمل بدون شاشة سوداء.
- `file_h.db` أو `file.db` يطابق `checksum_h.db` أو `checksum.db`.
- `maindata\file_browse_patterns.rda` يطابق الحزمة الجاهزة.
- يمكن الرجوع لآخر نسخة احتياطية من داخل الواجهة.

## نتيجة الفحص الحالي

- مجلد اللعبة المقروء: `D:\SteamLibrary\steamapps\common\Anno 117 - Pax Romana`
- قاعدة الملفات المستخدمة: `file_h.db`
- قاعدة التحقق المستخدمة: `checksum_h.db`
- حالة checksum: مطابق.
- حالة الحزمة المركبة: تطابق الحزمة الجاهزة.
- نسبة النصوص العربية الحالية: `95.21%`.
- آخر نسخة احتياطية مسجلة: `backups\existing-slot-arabic\20260430-214819`.

## ملاحظات النشر

- النسخة المحمولة موجودة داخل `Anno117ArabicManager\desktop-dist\Anno 117 Arabic Manager-win32-x64`.
- إذا نُقلت الأداة إلى جهاز آخر، يجب وضع ملف `game-path.txt` بجانب التطبيق أو بجانب الملف التنفيذي، ويحتوي مسار مجلد اللعبة.
- لا تنشر ملفات `maindata` الأصلية الخاصة باللعبة. الأداة يجب أن تبني الحزمة من نسخة المستخدم المحلية.

## تحديث الهوية البصرية

- تمت إضافة رأس علوي باسم اللعبة: `Anno 117: Pax Romana`.
- تمت إضافة العنوان العربي: `عام 117: السلام الروماني`.
- تمت إضافة التوقيع: `تعريب وإدارة Omar009`.
- تم تجهيز زر موسيقى للواجهة مع دعم ملف موسيقى محلي داخل `assets\audio`.
- في حال عدم وجود ملف موسيقى، تعمل أجواء موسيقية خفيفة مولدة داخل التطبيق.
